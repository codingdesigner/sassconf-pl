#!/bin/bash

#if [[ $1 = "rebase" ]]; then
#    echo "Begin post-rewrite git hook"
#    echo "Rebuiling compiled files post $1..."
#    bundle exec compass compile
#    php core/builder.php --generate --nocache
#
##    echo "Adding built files to the last commit"
##    git add -u
##    git commit --amend --no-edit
#fi
